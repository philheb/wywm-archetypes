import FormStyle from './FormStyle';

const Form = () => {
  return (
    <FormStyle>
      <h4>First things first, what's your name?</h4>
      <input type="text" placeholder='Name' />
    </FormStyle>
  );
}

export default Form;
